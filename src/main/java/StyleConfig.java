record StyleConfig(
        BasicColor basicColor,
        Rgb rgbColor,
        BasicColor bgBasicColor,
        Rgb bgRgbColor,
        boolean isBold,
        boolean isDim,
        boolean isItalic,
        boolean isUnderlined,
        boolean isSlowBlink,
        boolean isRapidBlink,
        boolean isReverse,
        boolean isHide) {
}
